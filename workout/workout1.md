// Create an array of your 5 favorite movies
// Add a new movie at the beginning
// Remove the last movie
// Print the length

// Create a "person" object with:
// - name, age, city
// - Add a method that returns a greeting
// - Add a new property "hobby"
// - Delete the "age" property

// Given: [2, 4, 6, 8, 10]
// Use map to create a new array with each number doubled
// Use map to convert numbers to strings with "Number: " prefix

// Given: [15, 22, 8, 31, 12, 45, 6]
// Filter numbers greater than 20
// Filter even numbers

// Given: [10, 20, 30, 40]
// Find the sum
// Find the product (multiply all numbers)

// Given array of objects: [{id:1, name:'Alice'}, {id:2, name:'Bob'}, {id:3, name:'Charlie'}]
// Find the object with id: 2
// Find the index of object with name 'Charlie'

// Given: [3, 5, 7, 9, 11]
// Check if every number is odd
// Check if some numbers are greater than 10

// Given: [45, 12, 78, 23, 56, 9]
// Sort numbers in ascending order
// Sort numbers in descending order

// Given object: {a:1, b:2, c:3}
// Get all keys using Object.keys()
// Get all values using Object.values()
// Get entries using Object.entries()

// Given array of products:
// [{name:'Laptop', price:999, inStock:true}, 
//  {name:'Phone', price:599, inStock:false},
//  {name:'Tablet', price:399, inStock:true}]
// 
// Tasks:
// 1. Filter in-stock products
// 2. Map to get only names and prices
// 3. Reduce to find total price of in-stock items
// 4. Check if any product costs less than 500

// Merge two arrays using spread: [1,2,3] and [4,5,6]
// Create a copy of an object and add a new property
// Use rest parameter in a function that sums any number of arguments


// Array destructuring: get first and third element from [10,20,30,40]
// Object destructuring: get name and city from {name:'John', age:25, city:'NYC'}
// Swap two variables using destructuring

// Given: [5, 12, 8, 130, 44, 3]
// Chain: filter > 10, map to double, sort descending
// Output should be [260, 88, 24]


// Use forEach to log each element of [1,2,3,4,5] multiplied by 3
// Use for...of to iterate over object keys and values

// Create a "student" array of objects with: name, grade, subjects (array)
// Tasks:
// 1. Find student with highest grade
// 2. Get all subjects from all students (flatten)
// 3. Filter students with grade > 70
// 4. Create an object with student names as keys and grades as values
// 5. Calculate class average